[train.data]
dataset_dir = "dataset"

[train.runtime]
out_dir = "out"
device = "cpu"
dtype = "float32"
compile = false

[sample.runtime]
out_dir = "out"
device = "cpu"
dtype = "float32"
compile = false
