name: Quality Gates

on:
  push:
    branches: [ master ]
  pull_request:

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: Quality (pre-commit gate)
    runs-on: ubuntu-latest
    env:
      PRE_COMMIT_HOME: .cache/pre-commit
      UV_CACHE_DIR: .cache/uv
      PIP_CACHE_DIR: .cache/pip
    steps:
      - uses: actions/checkout@v4
      - id: setup-python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: astral-sh/setup-uv@v4
      - uses: actions/cache@v4
        with:
          path: |
            .cache/uv
            .venv
            .venv312
          key: ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-uvenv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-uvenv-
      - name: Cache pre-commit envs
        uses: actions/cache@v4
        with:
          path: .cache/pre-commit
          key: ${{ runner.os }}-precommit-${{ hashFiles('.githooks/.pre-commit-config.yaml') }}-py${{ steps.setup-python.outputs.python-version }}
          restore-keys: |
            ${{ runner.os }}-precommit-
      - name: Cache uv global wheels
        uses: actions/cache@v4
        with:
          path: |
            .cache/uv
          key: ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-uvcache-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-uvcache-
      - name: Cache pip downloads
        uses: actions/cache@v4
        with:
          path: |
            .cache/pip
          key: ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-pip-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-pip-
      - name: Cache ruff cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ruff
            .cache/ruff
          key: ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-ruff-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-ruff-
      - run: uv sync --frozen
      - run: uvx --from . ci-tasks quality
